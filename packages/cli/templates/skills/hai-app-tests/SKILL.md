---
name: hai-app-tests
description: "编写或修复 hai 应用的 Vitest/Playwright 测试，验证业务边界和完整用户流程。"
---

# hai-app-tests

## 能力契约

| 项目 | 契约 |
| --- | --- |
| 能力 | 编写或修复 hai 应用的 Vitest/Playwright 测试，验证业务边界和完整用户流程 |
| 适用场景 | 需求验收、缺陷回归或应用测试改进 |
| 输入 | 验收条件、被测公共入口、现有测试和运行环境 |
| 输出 | 可复现用例及明确区分静态、mock、浏览器/外部服务的结果 |
| 限制 | Red 阶段仅要求新增目标用例因行为缺失而失败，不要求旧测试失败；不用删断言或修改预期掩盖缺陷。 |

## 使用路径

- 先选单元/集成/浏览器层，复用测试 fixture，验证用户可观察结果。
- 新用例已通过时先检查是否已有能力；不能为达成 Red 人为破坏旧测试。
- 外部服务未运行必须报告；mock/Testcontainers/浏览器/真实付费 Provider 的结果分开表述。

## 按需参考

详细配置、API 签名、错误码和范本在 [reference.md](reference.md)。按下列标题定位所需小节，不默认通读；用例中的业务变量需结合当前应用补齐。

- 适用场景
- TDD 核心工作流（必须严格遵循）
- 测试目录约定
- 核心规则
- 覆盖范围
- 单元测试（Vitest）
- E2E 测试（Playwright）
- TDD 用例设计策略
- 断言规范
- 质量门禁
- 相关 Skills
