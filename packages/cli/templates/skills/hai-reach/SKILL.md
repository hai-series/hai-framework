---
name: hai-reach
description: "使用 @h-ai/reach 配置邮件、短信、API 通知和多渠道消息模板。"
---

# hai-reach

## 能力契约

| 项目 | 契约 |
| --- | --- |
| 能力 | 使用 @h-ai/reach 配置邮件、短信、API 通知和多渠道消息模板 |
| 适用场景 | 配置业务通知、验证码渠道或发送模板 |
| 输入 | ReachConfigInput、Provider、模板、收件人与模板参数 |
| 输出 | 发送和模板操作的 HaiResult |
| 限制 | 仅服务端；模板绑定 Provider，遵守 DND。区分测试与真实发送，不记录验证码或收件人敏感内容。 |

## 使用路径

- 先配置并注册 Provider，再保存绑定 Provider 的模板；发送结果须检查，DND/禁用与成功发送区分。
- 验证模板替换和错误分支时使用隔离测试收件人；真实通知须在用户授权范围内。

## 按需参考

详细配置、API 签名、错误码和范本在 [reference.md](reference.md)。按下列标题定位所需小节，不默认通读；用例中的业务变量需结合当前应用补齐。

- 运行环境
- 依赖
- 适用场景
- 使用步骤
- 核心 API
- 错误码
- 常见模式
- 相关 Skills
