# AGENTS.md

Fullstack 多包工程入口。先读 README、根 workspace 脚本与相关 package/app；模块用法按需读取 `.agents/skills/*/SKILL.md` 及其引用。

## 行为契约

- 修改前检索实现、调用方、测试和文档，优先复用现有 contract、procedure、共享组件与脚本。
- 跨包变更先明确受影响的 contract、serv、shared、前端及交付流程，再同步调用方；不必输出固定问卷。
- 禁止 `any`、`console.log`、硬编码密钥。代码注释中文，日志英文；用户文本走 i18n。

## 架构边界

| 路径 | 职责 |
| --- | --- |
| `packages/{{projectName}}-contract` | API contract、Zod schema 与共享类型 |
| `packages/{{projectName}}-serv` | 使用 `@h-ai/serv` 实现后端业务 API |
| `packages/{{projectName}}-shared` | 共享 UI Shell、主题、i18n 和 typed API client |
| `apps/{{projectName}}-web` | Svelte 5 + Vite Web 前端，按选择生成 |
| `apps/{{projectName}}-app` | Svelte 5 + Vite + Capacitor 移动端 SPA，按选择生成 |
| `apps/{{projectName}}-desktop` | Svelte 5 + Vite + Tauri v2 桌面端，按选择生成 |
| `apps/{{projectName}}-miniapp` | 选择 miniapp 时生成的预留说明与文案，当前不是可运行应用 |

- 前端负责 UI 与请求编排；后端业务放 serv，共享能力放 shared。本样板默认不用 `@h-ai/kit`，不要混入 SvelteKit load / route 结构。
- 返回 `HaiResult<T>` 的业务 API 先判断 `success`，业务错误直接透传；纯函数、工厂和流按实际签名使用，不能假定都返回 HaiResult。
- 用户文本按 shared 跨端文案与各 app 专属文案分工，同步现有 locale。
- 认证接入前检查现有实现；样板不意味着已配好 Cookie 或安全 token 存储。新增认证时 Web 明确 Cookie/内存方案，App 原生端配置安全 TokenStorage，Desktop 按 Tauri 安全边界实现。

## 质量门禁

按影响范围执行，跨包契约从根工作区验证：

```bash
pnpm typecheck
pnpm lint
pnpm build
pnpm test
pnpm test:e2e
```

发布或原生壳改动另检查 Capacitor/Tauri 配置、根脚本，并按目标平台执行 `pnpm compile`、`pnpm package`。浏览器测试不能代替原生包验证。

## 完成条件

- 同步受影响的 contract、serv procedure、shared client、各端调用方、README、messages、skills 和测试。
- 报告实际门禁结果、受影响 package/app、未执行项及原因；不得将跳过或失败写成通过。

## 优先 Skills

构建与验收：`hai-build`、`hai-app-review`、`hai-app-tests`。模块使用：`hai-core`、`hai-ui`、`hai-serv`、`hai-api-contract`、`hai-api-client`、`hai-capacitor`；其余按需选择。
