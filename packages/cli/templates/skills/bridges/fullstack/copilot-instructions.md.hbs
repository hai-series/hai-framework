# GitHub Copilot 项目指令

共享工作规范见 [AGENTS.md](../AGENTS.md)，模块用法按需读取 `.agents/skills/`。

- `packages/{{projectName}}-contract` 定义 API；Fullstack 服务端 `packages/{{projectName}}-serv` 实现业务；`packages/{{projectName}}-shared` 共享 UI、i18n 和 typed client。
- 前端按选择生成；App 使用 Capacitor，Desktop 使用 Tauri v2。miniapp 当前仅预留说明，不是可运行应用。

## 质量门禁

按根 AGENTS 执行 typecheck、lint、build、test 和 test:e2e；原生或发布变更另执行对应 compile/package。

## 完成条件

同步受影响的 contract、serv、shared、各端调用方及文档与测试，报告门禁结果和未执行原因。
