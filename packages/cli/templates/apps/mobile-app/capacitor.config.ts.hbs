import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.hai.{{projectName}}',
  appName: '{{projectName}}',
  webDir: 'dist',
  server: {
    androidScheme: 'https',
  },
}

export default config