{{!-- 管理后台首页 --}}
<script lang='ts'>
  /**
   * 管理后台首页 — 入口导航
   */
  import { resolve } from '$app/paths'
{{#if hasI18n}}
  import * as m from '$lib/paraglide/messages'
{{/if}}
  import { Badge, Card } from '@h-ai/ui'
</script>

<svelte:head>
{{#if hasI18n}}
  <title>{m.app_title()}</title>
{{else}}
  <title>管理后台</title>
{{/if}}
</svelte:head>

<main class='min-h-screen flex items-center justify-center p-6'>
  <Card shadow='lg' padding='xl' class='w-full max-w-xl bg-base-100 text-center'>
{{#if hasI18n}}
    <div class='flex justify-center'>
      <Badge variant='primary'>
        <span class='icon-[tabler--sparkles] size-4'></span>
        {m.page_home_badge()}
      </Badge>
    </div>
    <h1 class='mt-4 text-4xl font-black tracking-tight'>{m.app_title()}</h1>
    <p class='mt-3 text-base-content/60'>{m.page_home_subtitle()}</p>
    <div class='mt-8 flex flex-wrap gap-3 justify-center'>
      <a href={resolve('/admin', {})} class='btn btn-primary'>
        <span class='icon-[tabler--layout-dashboard] size-5'></span>
        {m.nav_admin()}
      </a>
    </div>
{{else}}
    <div class='flex justify-center'>
      <Badge variant='primary'>
        <span class='icon-[tabler--sparkles] size-4'></span>
        由 hai 驱动
      </Badge>
    </div>
    <h1 class='mt-4 text-4xl font-black tracking-tight'>管理后台</h1>
    <p class='mt-3 text-base-content/60'>基于 hai Agent Framework 构建</p>
    <div class='mt-8 flex flex-wrap gap-3 justify-center'>
      <a href={resolve('/admin', {})} class='btn btn-primary'>
        <span class='icon-[tabler--layout-dashboard] size-5'></span>
        进入后台
      </a>
    </div>
{{/if}}
  </Card>
</main>
