import { describe, expect, it } from 'vitest'
import { AppEchoInputSchema, {{api.contractExportName}} } from '../src/index.js'

describe('{{api.contractPackageName}}', () => {
  it('includes generated contract routes', () => {
    expect({{api.contractExportName}}.app.info).toBeDefined()
    expect({{api.contractExportName}}.app.echo).toBeDefined()
{{#if features.iam}}    expect({{api.contractExportName}}.iam.auth.login).toBeDefined()
{{/if}}{{#if features.storage}}    expect({{api.contractExportName}}.storage.presignedUrls.createUpload).toBeDefined()
{{/if}}{{#if features.ai}}    expect({{api.contractExportName}}.ai.chats.createCompletion).toBeDefined()
{{/if}}  })

  it('validates app.echo input length', () => {
    expect(AppEchoInputSchema.safeParse({ message: 'hello' }).success).toBe(true)
    expect(AppEchoInputSchema.safeParse({ message: '' }).success).toBe(false)
    expect(AppEchoInputSchema.safeParse({ message: 'x'.repeat(2001) }).success).toBe(false)
  })
})
