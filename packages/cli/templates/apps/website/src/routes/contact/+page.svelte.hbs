{{!-- 联系页面 --}}
<script lang='ts'>
  /**
   * 联系我们页面
   */
{{#if hasI18n}}
  import * as m from '$lib/paraglide/messages'
{{/if}}
  import { Button, Card, FormField, Input, PageHeader } from '@h-ai/ui'

  let name = $state('')
  let email = $state('')
  let message = $state('')

  function handleSubmit(event: SubmitEvent) {
    event.preventDefault()
  }
</script>

<svelte:head>
{{#if hasI18n}}
  <title>{m.page_contact_title()}</title>
{{else}}
  <title>联系我们</title>
{{/if}}
</svelte:head>

<section class='py-16 px-4 max-w-2xl mx-auto'>
{{#if hasI18n}}
  <PageHeader title={m.page_contact_title()} description={m.page_contact_desc()} />
{{else}}
  <PageHeader title='联系我们' description='有任何疑问？欢迎联系我们。' />
{{/if}}

  <Card shadow='sm' class='bg-base-100'>
    <form class='space-y-4' onsubmit={handleSubmit}>
{{#if hasI18n}}
      <FormField label={m.page_contact_name()} required>
        <Input id='name' type='text' bind:value={name} required />
      </FormField>
      <FormField label={m.page_contact_email()} required>
        <Input id='email' type='email' bind:value={email} required />
      </FormField>
      <FormField label={m.page_contact_message()} required>
        <textarea id='message' class='textarea textarea-bordered w-full' rows='4' bind:value={message}></textarea>
      </FormField>
      <Button type='submit' variant='primary' class='w-full'>{m.page_contact_submit()}</Button>
{{else}}
      <FormField label='姓名' required>
        <Input id='name' type='text' bind:value={name} required />
      </FormField>
      <FormField label='邮箱' required>
        <Input id='email' type='email' bind:value={email} required />
      </FormField>
      <FormField label='留言' required>
        <textarea id='message' class='textarea textarea-bordered w-full' rows='4' bind:value={message}></textarea>
      </FormField>
      <Button type='submit' variant='primary' class='w-full'>发送</Button>
{{/if}}
    </form>
  </Card>
</section>
