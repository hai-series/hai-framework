{{! @skipIfEmpty }}
{{#if fullstack.frontends.app}}
/**
 * {{projectName}}-app — Capacitor SPA 模式布局。
 *
 * prerender = true, ssr = false：输出纯静态 SPA，由 Capacitor 包装。
 */

export const prerender = true
export const ssr = false
{{/if}}
