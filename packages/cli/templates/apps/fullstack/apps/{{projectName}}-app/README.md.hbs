{{! @skipIfEmpty }}
{{#if fullstack.frontends.app}}
# {{projectName}}-app

App/H5 前端工程，使用 `@h-ai/api-client` 绑定共享 contract 调用后端 API，并通过 `@h-ai/ui` 组件搭建页面。

## 开发

- `pnpm --filter {{projectName}}-app dev`，默认端口 4174
- `pnpm --filter {{projectName}}-app test`
{{/if}}
