{{! @skipIfEmpty }}
{{#if fullstack.frontends.app}}
# {{projectName}}-app

App/H5 前端工程，参考 `apps/mobile-app` 的移动端 UI 风格，使用 Svelte 5 + Vite + Capacitor，可输出 Web 预览并同步 Android / iOS 原生工程。

## i18n 与 shared 协同

- 本端页面文案：`messages/{locale}.json` + `project.inlang/settings.json`
- 共享 Shell/主题/语言/API：`packages/{{fullstack.sharedPackageName}}`
- 语言切换通过 shared runtime 与本端 `src/lib/paraglide/runtime.js` 同步。
- 设置 tab 默认集成 shared 的 `ThemeSwitcher` / `LanguageSwitcher`，可直接验证主题与语言同步。

自定义建议：移动端独有引导、运营位、按钮文案放在本目录 `messages/`；跨端通用组件放到 shared。

## 编译、打包与部署

```bash
# 开发预览，默认端口 {{fullstack.ports.app}}
pnpm --filter {{projectName}}-app dev

# 编译静态 SPA 产物到 dist/
pnpm --filter {{projectName}}-app build

# 同步原生工程；首次使用前按需执行 cap:add:android / cap:add:ios
pnpm --filter {{projectName}}-app package

# Android / iOS 原生打包
pnpm --filter {{projectName}}-app cap:build:android:debug
pnpm --filter {{projectName}}-app cap:build:android:release
pnpm --filter {{projectName}}-app cap:build:ios

# 当前模板的 deploy 入口默认执行 package，可按目标商店或 CI 平台替换
pnpm --filter {{projectName}}-app deploy
```

部署前确认 `PUBLIC_API_BASE` 在 `.env` 中指向可访问的后端服务。Android 模拟器访问宿主机通常使用 `http://10.0.2.2:3000`。
{{/if}}
