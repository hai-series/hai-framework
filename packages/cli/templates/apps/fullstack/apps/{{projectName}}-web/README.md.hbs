{{! @skipIfEmpty }}
{{#if fullstack.frontends.web}}
# {{projectName}}-web

Web 前端工程，使用 `@h-ai/api-client` 绑定共享 contract 调用后端 API，并通过 `@h-ai/ui` 组件搭建页面。

## 开发

- `pnpm --filter {{projectName}}-web dev`，默认端口 4173
- `pnpm --filter {{projectName}}-web test`
{{/if}}
