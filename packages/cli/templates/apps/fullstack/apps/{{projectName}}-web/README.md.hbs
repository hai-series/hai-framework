{{! @skipIfEmpty }}
{{#if fullstack.frontends.web}}
# {{projectName}}-web

Web 前端工程，使用 `@h-ai/api-client` 绑定共享 contract 调用后端 API，并通过 `@h-ai/ui` 组件搭建页面。

## i18n 与 shared 协同

- 本端页面文案：`messages/{locale}.json` + `project.inlang/settings.json`
- 共享 Shell/主题/语言/API：`packages/{{fullstack.sharedPackageName}}`
- 语言切换由 shared 的 `LanguageSwitcher` 触发，并同步本端 `$lib/paraglide/runtime.js`

自定义建议：Web 独有文案放在本目录 `messages/`；跨端通用文案或组件放到 shared。

## 编译、打包与部署

```bash
# 开发预览，默认端口 {{fullstack.ports.web}}
pnpm --filter {{projectName}}-web dev

# 编译 Web 前端
pnpm --filter {{projectName}}-web build

# 打包部署产物；默认等同 build，可按目标平台替换
pnpm --filter {{projectName}}-web package

# 当前模板的 deploy 入口默认执行 package，可替换为 CDN / 容器发布脚本
pnpm --filter {{projectName}}-web deploy
```
{{/if}}
