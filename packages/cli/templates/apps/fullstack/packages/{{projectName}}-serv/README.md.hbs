# {{fullstack.servPackageName}}

后端 API Service，基于 `@h-ai/serv` 将共享 contract 装配为最小 HTTP App 抽象，不在应用代码中直接暴露 Hono。

## 骨架说明

- `config/_core.yml`：应用元信息（名称 / 版本 / 环境 / 语言），启动时由 `core.config` 加载。
- `config/_serv.yml`：服务端运行时设置（CORS 来源等）。
- `src/index.ts`：进程入口，先 `core.init({ configDir: './config' })` 加载配置再启动监听。
- `src/app-server.ts`：服务装配入口，挂载健康检查、OpenAPI、docs 与 procedures；应用名称 / 版本与 CORS 来源均取自上述配置。
- `src/server/procedures/app-procedures.ts`：app 领域 procedures，对应 contract 中的 `app.info` / `app.echo` 路由。
- `tests/app-server.test.ts`：通过 `ServerApp.request()` 验证健康检查与业务路由。

## 开发

- `pnpm --filter {{fullstack.servPackageName}} dev`
- `pnpm --filter {{fullstack.servPackageName}} test`

## 容器运行

- 根目录 `pnpm docker:build` / `pnpm podman:build` 生成 Web + Service 同源镜像；`pnpm docker:build:service` / `pnpm podman:build:service` 只生成本服务镜像。
- 合并镜像的配置挂载点为 `/app/service/config`，数据目录为 `/app/service/data`；service-only 镜像对应 `/app/config` 与 `/app/data`。
- YAML 配置使用 `${ENV_NAME:default}` 接收运行时环境变量；不要把密钥写入镜像。
- 将 SQLite/本地存储放入本目录 `data/` 后运行 `pnpm docker:build:data` / `pnpm podman:build:data`，可生成带初始数据的镜像，持续写入仍应使用 volume。
