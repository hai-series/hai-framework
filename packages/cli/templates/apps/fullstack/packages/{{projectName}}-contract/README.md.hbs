# {{fullstack.contractPackageName}}

共享 API 契约包，包含 Zod Schema、`apiContract.route()` 业务接口定义与工程级 contract。

## 骨架说明

- `src/app-schemas.ts`：跨接口、跨层复用的输入/数据 Schema 与类型。
- `src/app-contract.ts`：app 领域 contract；每个业务 path 直接写在对应接口定义中。
- `src/{{projectName}}-contract.ts`：工程级 contract，供 `@h-ai/serv` 与 `@h-ai/api-client` 共用。

只使用一次的输出 Schema 直接内联到 `.output(...)`；同一 contract 文件内重复使用时才保留私有常量。
API 前缀、健康检查与跨端 Header 等传输配置位于 serv 包的 `config/_serv.yml`。

## 开发

- `pnpm --filter {{fullstack.contractPackageName}} typecheck`
- `pnpm --filter {{fullstack.contractPackageName}} test`
