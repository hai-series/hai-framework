import process from 'node:process'
import { defineConfig } from '@playwright/test'

export default defineConfig({
  testDir: './e2e',
  fullyParallel: false,
  forbidOnly: !!process.env.CI,
  retries: process.env.CI ? 2 : 0,
  workers: 1,
  reporter: 'list',
  timeout: 30_000,
  use: {
    baseURL: '{{#if fullstack.e2eFrontend}}http://127.0.0.1:{{fullstack.e2eFrontend.port}}{{else}}http://127.0.0.1:3000{{/if}}',
    channel: 'chrome',
    trace: 'on-first-retry',
    screenshot: 'only-on-failure',
  },
  webServer: [
    {
      name: '{{fullstack.servPackageName}}',
      command: 'pnpm --filter {{fullstack.servPackageName}} start',
      url: 'http://127.0.0.1:3000/health',
      reuseExistingServer: false,
      timeout: 120_000,
    }{{#if fullstack.e2eFrontend}},
    {
      name: '{{fullstack.e2eFrontend.packageName}}',
      command: 'pnpm --filter {{fullstack.e2eFrontend.packageName}} preview',
      url: 'http://127.0.0.1:{{fullstack.e2eFrontend.port}}',
      reuseExistingServer: false,
      timeout: 120_000,
    }{{/if}},
  ],
})
