/**
 * =============================================================================
 * @h-ai/ui - 场景组件 (Scenes)
 * =============================================================================
 *
 * 面向具体业务场景的完整 UI 流程，通常对应一个完整的用户任务。
 *
 * 包含：
 * - iam/ - 身份认证场景（登录、注册、密码管理等）
 * - storage/ - 存储管理场景（文件上传、列表、图片上传等）
 * - crypto/ - 加密展示场景（加密输入、哈希展示、签名展示等）
 * =============================================================================
 */

// App 应用级场景组件
export * from './app'

// Crypto 场景组件
export * from './crypto'

// IAM 场景组件
export * from './iam'

// Storage 场景组件
export * from './storage'

// 场景组件类型
export * from './types'
