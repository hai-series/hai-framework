/**
 * =============================================================================
 * @hai/core - Functions 统一导出。
 * =============================================================================
 * 注意：Logger 函数需要根据环境分别从 node.ts 或 browser.ts 导入。
 *
 * @example
 * ```ts
 * import { config, id } from '@hai/core'
 * ```
 * =============================================================================
 */

/**
 * 配置管理（Node.js 专用，浏览器环境下会抛出错误）。
 */
export * from './core-function-config.js'

/**
 * ID 生成。
 */
export * from './core-function-id.js'
