/**
 * @h-ai/scheduler — 内部常量
 *
 * 本文件定义模块内部使用的常量，不通过 index.ts 对外导出。
 * @module scheduler-constants
 */

// ─── 固定表名常量 ───

/** 执行日志表名 */
export const SCHEDULER_LOG_TABLE = 'hai_scheduler_logs'

/** 任务定义表名 */
export const SCHEDULER_TASK_TABLE = 'hai_scheduler_tasks'
