/**
 * @h-ai/ai/client — 前端 AI 客户端入口
 *
 * 通过 `@h-ai/ai/client` 导入，提供 `createAIClient`、`parseSSE`、`collectStreamContent` 等浏览器端 API。
 * @module index
 */

export * from './ai-a2a-client.js'
export * from './ai-client.js'
