# =============================================================================
# Corporate Website - 存储配置（合作登记归档）
# =============================================================================

type: ${HAI_STORAGE_TYPE:local}
root: ${HAI_STORAGE_PATH:./data/uploads}
