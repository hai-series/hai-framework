# =============================================================================
# API Service - 数据库配置
# =============================================================================

type: ${HAI_DB_TYPE:sqlite}
database: ${HAI_DB_DATABASE:./data/api-service.db}
