/**
 * =============================================================================
 * Admin Console - 用户类型定义
 * =============================================================================
 *
 * 用户管理已完全交由 @h-ai/iam 模块处理，此文件仅保留必要类型定义。
 * =============================================================================
 */

import type { User } from '@h-ai/iam'

/**
 * 带角色和权限的用户
 */
export interface UserWithRoles extends User {
  roles: string[]
  permissions: string[]
}
