# =============================================================================
# Admin Console - 缓存配置
# =============================================================================

# 缓存类型: memory | redis
type: ${HAI_CACHE_TYPE:memory}

# Redis 配置（当 type 为 redis 时使用）
# redis:
#   url: ${HAI_CACHE_REDIS_URL:redis://localhost:6379}
#   prefix: hai:
